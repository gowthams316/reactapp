 },
 "scripts": {
    "build": "gatsby build"
    "develop": "gatsby develop"
    "format": "prettier --write \"**/*.{js,jsx,json,md}\"",
    "start": "npm run develop"
    "serve": "gatsby serve"
    "test": "jest --watch"
    "test:ci": "jest"
 }
}
